# 投稿联系与榜单验收

- API 6 文件 43 项通过：`community-api`、`profile-api`、`api`、`permissions`、`session-capabilities`、`repository`。修正历史题包分类后，`community-api` 的 3 项再次通过，包括真实 PGlite 数据库排序分页、删除后计数更新、空页总数。
- 联系资料覆盖未登录、普通用户、明确拒绝、机器人、有效模拟登录的越权拒绝及授权读取；响应不缓存，不混入权限或令牌字段。
- 榜单无需登录，三种排序、分页、严格查询参数和输出白名单通过；合成私题题名/题面/姓名/QQ 不出现在响应中。
- Web 全量 24 文件 154 项通过；API 构建、Web 类型检查与构建另由发布检查运行。
- 桌面和手机浏览器共 6 项通过：游客榜单排序分页及长昵称、真实 API 的题目/用户管理联系方式入口、切换普通账号后的不可读、中文输入法候选期间零搜索请求且确认后完整词语提交并保持焦点。观察截图后修正游客页顶部登录按钮拉伸，另行复验两种尺寸。
- 历史迁移题虽沿用 `problem-package` 类型，其 `importSource` 与常规题目包入口不同；聚合只接受 native 或常规 `problem-package` 来源，不改动历史业务记录。生产只读核对现有 156 条包导入均属历史来源，另有 3 条占位符。

没有在生产创建合成题或修改联系方式；上线后的真实页面与接口检查记录在部署交接中。账号密码、邮箱变更、第三方关联、比赛导出与题号迁移仍是后续事项，不属于本批完成声明。
