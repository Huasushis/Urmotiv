# 服务器目录归并核验（2026-07-31）

## 目的

把本项目原先散落在 `/home/ubuntu` 下的三个顶层目录整理成唯一入口 `/home/ubuntu/codex-urmotiv`，同时完整保留仓库代码、实验环境、实验结果、私有资料、浏览器程序和 Linux 工具。主目录中的其他项目与文件不在迁移范围内。

## 移动映射

| 原路径 | 新路径 | 方法 |
| --- | --- | --- |
| `/home/ubuntu/urmotiv-codex` | `/home/ubuntu/codex-urmotiv/previous-server-work/urmotiv-codex` | 同一文件系统内原地移动 |
| `/home/ubuntu/codex-urmotiv-database-foundation-019f99c1` | `/home/ubuntu/codex-urmotiv/previous-server-work/codex-urmotiv-database-foundation-019f99c1` | 同一文件系统内原地移动 |
| 主目录中的空白名称异常目录 | `/home/ubuntu/codex-urmotiv/previous-server-work/malformed-empty-directories-20260730` | 确认没有文件和链接后原地移动 |

“同一文件系统内原地移动”只改变目录入口，不复制文件内容。目录编号保持不变，可证明移动的是同一个目录，而不是重新生成的近似副本。

## 移动前后证据

### 旧服务器工作目录

- 设备编号：`64771`，目录编号：`265867`，移动前后相同。
- 普通文件：移动前后均为 `65,857`。
- 符号链接：移动前后均为 `2,736`。符号链接是指向另一文件位置的短引用，核验它可以发现依赖目录是否在移动后失效。
- 目录：移动前后均为 `8,889`。
- 移动前表观大小：`3,637,402,844` 字节。
- 移动后表观大小：`3,637,402,949` 字节。
- 大小增加 `105` 字节，原因是三个绝对链接改用了更长的新路径；文件数量没有变化。
- 三个绝对链接已精确改到新归档内，其中两个属于 Fermata 实验副本的 `node_modules`，一个属于私有环境文件引用。
- 改写后失效链接数量：`0`；仍指向旧顶层路径的绝对链接数量：`0`。

### 数据库基础实验副本

- 设备编号：`64771`，目录编号：`295167`，移动前后相同。
- 普通文件：移动前后均为 `27,627`。
- 符号链接：移动前后均为 `1,182`。
- 目录：移动前后均为 `4,323`。
- 表观大小：移动前后均为 `658,037,856` 字节。
- 移动后失效链接数量：`0`。

### 空白名称异常目录

- 原目录编号：`921438`，移动后保持相同。
- 普通文件：`0`；符号链接：`0`；目录：`53`。
- 已移入 `previous-server-work/`，不再占用用户主目录的异常名称。

## 进程与边界检查

- 移动前检查了所有进程的工作目录和已打开文件，没有进程使用两个旧项目目录。
- 三个目录都位于设备编号 `64771` 的同一文件系统。
- 没有删除任何仓库、实验结果、私有资料、数据库测试状态、浏览器程序或 Linux 工具。
- `/home/ubuntu` 下除上述三个明确来源外的文件和项目没有移动。`/home/ubuntu/cc.sql` 明确不属于本项目，不在迁移范围内。

## 当前唯一入口

```bash
cd /home/ubuntu/codex-urmotiv
codex
```

接手者先读根目录 `README.md`、`AGENTS.md` 和 `HANDOFF.md`。正式开发只使用根目录三个 Git 仓库；`previous-server-work/` 默认只读。
