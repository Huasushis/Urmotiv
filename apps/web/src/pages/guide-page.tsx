import { Link, useSearchParams } from "react-router-dom";
import { BookOpen } from "lucide-react";
import { MarkdownPreview } from "../components/markdown-editor";
const guides = [
  {
    id: "author",
    name: "投稿人",
    body: `## 从一个想法开始

1. 注册并完成邮箱验证，再用用户名或邮箱登录。在头像菜单的「个人资料」填写昵称、QQ 和邮箱，方便管理员联系。
2. 打开「我的投稿 → 新建题目」，填写名称、类型、知识点、基础题面和基础题解。也可下载 Markdown 模板，填完上传，先识别预览，再填入表单。
3. 创建后是草稿。在题目工作区补充内容并保存，检查无误后点击「提交审核」。只有创建权限不等于能查看别人的题目。
4. 从「我的投稿」查看状态和公开审核意见。不通过后修改基础内容，保存并重新提交；新的审核轮次不覆盖旧意见。

编辑时停顿后自动保存，持续输入时每 15 秒保存一次。注意右上角保存状态；出现版本冲突时不会覆盖别人的修改，请先保留自己的文本再重新读取。

启用「AI 快速建题」插件后，可以粘贴整段材料让模型识别各部分。系统按原文行号提取，预览确认后填入草稿表单。标程和未分类原文放入正式题解，避免意外进入题面；检查知识点并创建草稿后仍可修改。

## 基础内容和正式内容有什么区别？

| 内容 | 写什么 | 什么时候能修改 |
| --- | --- | --- |
| 基础题面 | 让审题人理解问题的核心任务、条件与输入输出 | 草稿/不通过时可改；待审和通过时冻结 |
| 基础题解 | 核心算法、正确性理由、复杂度及关键边界 | 同基础题面 |
| 正式题面 | 面向比赛选手的完整叙述、背景、输入输出与数据范围 | 有编辑权时可继续完善 |
| 正式题解 | 面向赛后读者的完整推导、证明和实现说明 | 有编辑权时可继续完善 |

题名始终不是冻结字段。冻结是保留审题时认可的核心，不是禁止修正所有文字；必须修改冻结内容时请联系有紧急修改权限的管理员，填写修改原因。

## AI 审题和人工意见

「允许 AI 审题」默认开启，提交后允许授权的外部服务读取本题的题面与题解；可以在创建时或工作区概要关闭。关闭不会删除已有意见。AI 只是意见来源，是否计入通过人数由站点规则决定，不代表一定准确；难题或简单题都不天然等于好题或坏题。

## 常用入口

- 头像菜单：修改资料、头像、密码、验证更换邮箱；学校认证启用后可主动绑定/解绑，不会因为同邮箱自动合并账号。
- 铃铛：查看全部通知、标记已读。新人公告可弹窗提醒。
- 个人资料 → 邮件通知：新审核意见、审核通过、审核不通过默认发到已验证的联系邮箱，可以逐类关闭。修改同一条意见不会反复发“新审核”邮件。
- 题目内容支持 Markdown 与数学公式；只向有权查看此题的人展示。不要把私有题面或未公开数据放到公开图床。
`,
  },
  {
    id: "member",
    name: "命题组成员",
    body: `## 先看投稿人指南

[投稿流程与两类题解的区别](/guide?role=author)同样适用。命题组成员通常还可查看、审核和完善团队题目；实际能力由管理员分配，某项明确拒绝仍优先。

## 审阅一道题

1. 从「待审」打开题目，先核对基础题面与基础题解，再检查正式内容、样例和数据范围。
2. 在「审核」填写自己的结论、难度、质量、原创性、知识点及改进点。主要改进点和公开评论对能查看题目的人可见；内部备注不要写到公开评论。
3. 评价支持 Markdown、列表、代码和数学公式。只能创建或修改自己的当轮意见；提交后会重新计算审核规则，可能立即改变题目状态。
4. 已关闭轮次只读。原题检索候选可展开查看来源和题面，相似度只是参考，不应单靠一个分数认定重复。

题库可筛选「待审且本轮我未审」和「本轮我已审」，列表显示已审标识。累计题数按题目去重；审题人榜可按数量或结果一致率排序。一致率只比较当前已定案轮次中明确通过/不通过的人工意见与最终结果，待审、旧轮次和“需要修改”不计入分母，不等于独立准确性评测。

## 难度与资料

- CF 难度：800–3500，步长 100；用于参考，不是通过阈值。
- 思维难度、代码难度、质量、原创性：1–5；缺少证据时不要凭空补历史分数。
- 知识点按大类展开，可多选。上传图片要走题目附件入口；测试数据、标准程序等是内部资料，查看题面不自动授予下载权限。
- Urmotiv 不执行选手代码。评测配置与数据可以维护、导出到 OJ，但页面不会替你验证程序正确性。

## 共同完善和组题

编辑前确认正在修改哪一分区；保存冲突时重新读取最新版本，不要覆盖别人的更改。已有未保存内容时先保存或放弃，再执行审核、状态切换等操作。

「组题」从已通过题目建立方案，可设顺序、分值与成员。加入时固定题目版本，题库后续修改不会自动替换方案内容。访问记录可辅助检查参赛人员是否看过题目；没有记录不等于绝对安全。

有权限者可以锁定/取消锁定、归档/取消归档。删除需要单独权限、确认后没有回收站，但不会删除题库里的题目。
`,
  },
  {
    id: "leader",
    name: "组长",
    body: `## 在成员工作之外

先阅读[命题组成员指南](/guide?role=member)。组长通常还负责最终状态、单题授权、组题与交付；组长身份不会自动获得系统密钥或所有后台管理权。

## 审核与状态

在工作区执行有权限的状态操作，或进入「管理 → 题目管理」筛选、选择多题并批量更改状态。批量操作不会跳过每道题的权限、版本与当前状态检查。

自动规则与人工最终确认是两件事：站点可按通过/拒绝人数计算结果，也可以决定是否计入机器人意见。旧轮次采用当时冻结的规则，后续改设置不篡改历史。审核后确认评分或标签写回时，核对当前轮次和字段；历史缺失值不应随意补造。

## 比赛交付

1. 从已通过题目建立方案，核对题目版本、顺序、分值、参与人员与泄题风险。
2. 定稿时锁定。需要改编排，先取消锁定；比赛结束后归档。取消归档回草稿但不悄悄更新固定题目版本。
3. 点击「导出比赛题目包」，选择已启用的 Hydro、FPS 等格式，阅读损失提示后创建任务。
4. 任务完成再下载，在目标 OJ 检查题面、数据、评测配置及题目顺序。格式能导出不代表每种 OJ 都支持全部特性。

删除比赛会撤销其已有导出链接，题库原题保留；只有想移除方案时才删除，想保留历史应归档。

## 导入、权限与协作

「导入导出」可上传 Hydro 多题 ZIP 或 FPS XML，先检测预览再确认。模板投稿只是填写单题草稿，不等于绕过正式题包导入权限。更新已有题目还需相应编辑权。

按需要给成员单题授权，遵循最小必要范围；不应为查看某一道题而直接开放全部私题。查看题面、编辑题目、内部附件和导出是不同权限。

有「管理公告」权限时可发布、置顶或撤回公告，指定新人、所有登录用户或权限组。可在 Markdown 正文附上 [使用指南](/guide)，也可以链接本页。没有该权限时请管理员授权，而不是共用账号。
`,
  },
  {
    id: "admin",
    name: "管理员",
    body: `## 账号和权限

先了解[组长的业务流程](/guide?role=leader)。管理员负责系统运行，不需要代替每位成员审题。

在「用户管理」查找账号，修改其权限组与个人允许/拒绝增量；「角色与权限」定义权限组本身，「默认权限」决定新账号的初始角色。允许可以叠加，任何匹配的明确拒绝优先；机器人还有无法解除的危险操作限制。修改权限不能授予固定 root 身份。

如获模拟登录权限，可从用户管理切换到普通真人账号；切换后只有目标账号权限。要返回必须重新登录，不能在模拟会话改目标密码或继续切换。

普通账号可自行修改密码、验证更换邮箱和邮件找回。root 使用用户名 root 与独立本地密码，即使关闭普通登录仍可用；忘记 root 密码需在服务器执行恢复命令。完整备份仅 root 直接登录可管理。

## 系统设置

- 常规设置：登录与注册开关、站点地址、SMTP 发信。公开地址须与部署 Web origin 匹配，改域名时先改部署配置。
- SMTP 密码一般是邮箱服务商的授权码；连接参数、发信人和加密方式须与服务商一致。邮件验证成功前账号不能正常登录。
- 统一身份认证：先拿到合法客户端配置，再启用。学校身份以提供方与稳定 id/gid 唯一识别，与站内账号主动关联；学号、姓名和学校邮箱独立显示，不强行覆盖本地资料。
- 服务账号：为外部程序创建机器人，授予必要权限并生成独立令牌。令牌只在生成时显示；撤销/轮换后同步更新调用方。

## 公告与新人引导

「管理 → 公告管理」可创建草稿、发布、置顶、选择受众和未读弹窗。新人指注册未超过所设天数的真人用户；指定权限组按当前有效成员资格判定，已撤销的成员不可读。默认受众是新人，默认期限 30 天。

取消「立即发布」再保存就是撤回，之后可重新发布。保存内容会产生新版本，接收人需要重新确认；不在每次页面刷新时重复弹已读版本。铃铛中可读全部仍对自己可见的公告。外部链接和原始 HTML 不会绕过 Markdown 安全规则。

## 插件与 Fermata

从「插件」启停并配置各插件。项目链接只负责跳转仓库，不会安装代码。密钥输入留空保持，清除须明确确认；不要把服务认证令牌当作模型密钥。

AI 快速建题有独立的模型接口地址、模型名称、深度思考开关和模型 API 密钥。兼容 OpenAI 的地址通常以 /v1 结尾；可以填写与 Fermata 相同的模型配置，但两个插件独立保存。仅有创建权限的真人账号可使用，结果只对发起账号可见；重启后未确认的临时结果不保留。

Anklang 默认可以只查 yuantiji，此模式不需要本地嵌入配置。选择本地/混合检索才配置兼容 OpenAI 的 embedding 接口；改变提供方后检查连接和重建状态。

Fermata 是独立服务，用机器人账号领取允许 AI 审题的待审题目。管理页配置模型地址、模型 ID、密钥、机器人令牌、并发和启用状态；查看运行日志可区分领取、处理、提交、认证与模型错误。日志只展示安全事件与错误码，不含题面、题解或模型原文；重启会清空最近日志。日志页并不是重跑历史题目的开关。

模型意见不等于已证明准确；是否计入通过结果由审核规则控制。在途任务使用领取时配置，不要为了刷新页面反复轮换令牌或重启服务。

## 备份、恢复与故障

在「备份与恢复」验证 WebDAV 连接后启用，保存到 urmotiv/ 文件夹。备份密码与 WebDAV 密码、root 密码相互独立。修改备份密码只影响新文件，务必保留旧密码。

完整站内备份包括数据库、账号密码散列、题目修订、审核、比赛、头像和有效附件；独立 Fermata/Anklang 的自有配置与数据、部署环境需另外备份。

恢复前核对文件时间、备份密码与当前 root 密码，输入确认文本。系统先校验、临时库演练、保存当前快照，再替换数据库；恢复后旧登录失效。不要用旧本地数据库覆盖生产库。

排错先看错误码、任务状态、插件连接与安全运行日志。报告问题时附操作步骤与版本，不贴私题、密钥、Cookie 或模型原文。更详细的服务器命令见 [管理员手册](https://github.com/Huasushis/Urmotiv/blob/main/docs/admin-guide.md)。
`,
  },
];
export function GuidePage() {
  const [params, setParams] = useSearchParams();
  const selected =
    guides.find((item) => item.id === params.get("role")) ?? guides[0]!;
  return (
    <section className="guide-page">
      <header className="page-heading">
        <div>
          <p className="eyebrow">Urmotiv</p>
          <h1>
            <BookOpen size={25} />
            使用文档
          </h1>
          <p>从投稿到比赛交付，按你的职责查阅。</p>
        </div>
        <Link to="/problems">返回题目</Link>
      </header>
      <nav className="guide-tabs" aria-label="使用文档角色">
        {guides.map((item) => (
          <button
            key={item.id}
            aria-pressed={selected.id === item.id}
            onClick={() => setParams({ role: item.id })}
          >
            {item.name}
          </button>
        ))}
      </nav>
      <article className="guide-content">
        <figure className="guide-screenshot"><a href={`/guide/${selected.id}.jpg`} target="_blank" rel="noreferrer"><img src={`/guide/${selected.id}.jpg`} alt={`${selected.name}操作入口（合成演示数据）`} loading="lazy"/></a><figcaption>合成演示界面，点击查看大图。具体按钮取决于账号权限。</figcaption></figure>
        <MarkdownPreview value={selected.body} />
      </article>
      <details className="guide-format">
        <summary>Markdown 与模板格式</summary>
        <MarkdownPreview
          value={
            "行内公式使用 `$x_i^2$` 或 `\\(x_i^2\\)`，独立公式使用 `$$` 分行包围，或 `\\[...\\]`。代码使用反引号，多个步骤用列表。\n\n模板首行为 `# 题目名称`，后续按空白模板中的二级标题填写。段落内的小标题使用三级标题，代码块内标题不会误识别。基础题面、基础题解必填，正式分区可暂留空；样例输入输出成对填写。识别预览确认后才填表，不会立即写入题库。类型、标签、难度与 AI 开关仍在创建表单选择。"
          }
        />
      </details>
    </section>
  );
}
