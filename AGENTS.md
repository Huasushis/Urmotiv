# Urmotiv 开发约定

本文件适用于仓库中的全部代码和文档。开始工作前先阅读 `docs/spec.md`、
`docs/permissions.md`、`docs/problem-package.md` 和 `docs/plugins.md`。

## 不可违反的规则

1. 不得提交 `.env`、密钥、访问令牌、真实账号信息、历史题目、题目表格、测试数据、
   题面附件或从这些资料生成的中间文件。`USTC题目列表.xlsx` 与 `hist_problem/` 只能留在本机。
2. 不得把自行编写的公开题目抓取插件提交到公开仓库。使用或改编开源项目时，先核对许可证，
   保留来源和许可证说明。
3. 不得为了显得专业而拼造新词，也不要把多个专业术语硬拼成一个名称。必须使用专业术语时，
   第一次出现就用日常语言解释它是什么、为什么需要它。界面文案尤其要让没有后端经验的人看懂。
4. 权限检查必须在服务端完成。隐藏按钮不是权限控制。任何根据题目编号读取、修改、下载或导出的
   接口都必须检查当前用户对该题目的权限，普通用户不得得知无权查看的题目是否存在。
5. 明确拒绝永远优先于允许。机器人账号还有一组不能被角色或插件解除的限制，禁止删除题目、
   删除用户、模拟其他用户登录、管理权限、管理系统和读取密钥。
6. 不得在日志、错误信息、监控事件或任务结果中输出题面全文、题解、测试数据、密码、令牌或密钥。
7. 导入压缩包必须防止目录跳转、符号链接、压缩炸弹、重名覆盖和超限文件。导出任务在创建和下载时
   都要重新检查权限。
8. 修改权限、身份认证、附件访问、导入导出或插件钩子时，必须增加越权与失败路径测试。

## 产品边界

- Urmotiv 是题库和命题协作系统，不包含代码评测执行器。
- 核心功能包括账号、权限、投题、审题、组题、题目访问记录、完整题目包导入导出和插件宿主。
- Anklang 是独立的原题检索服务；Fermata 是独立的 AI 审题服务。它们通过带版本号的 HTTP 接口
  与 Urmotiv 通信，不共享数据库。
- 图片只用于借鉴工作台布局、分区编辑和数据点表格。不要照搬双语字段、超长单页表单或重复保存按钮。
- 题面与题解使用 Markdown；桌面端支持编辑与预览并排，窄屏使用切换视图，并支持 KaTeX 数学公式。

## 工程约定

- 使用 TypeScript，开启严格类型检查。运行时输入必须通过共享的数据结构校验，不能只依赖 TypeScript 类型。
- Web、API、后台任务、共享数据结构和插件开发包放在独立工作区，避免互相读取内部文件。
- 生产环境使用 PostgreSQL、Redis 和兼容 S3 的文件存储。本地没有 Docker 时使用文档规定的轻量模式，
  不得要求开发者为了打开界面先安装 Docker。
- 本项目的构建、测试、数据库迁移、服务启动和浏览器联调统一在 `ssh ustc` 指向的服务器进行。
  本机只编辑文件和做不执行项目代码的静态检查，不在本机安装依赖或启动测试服务。
- 真实题目或密钥只在确有测试需要时通过 `scp` 传到服务器的非 Git 私有目录。传输前检查绝对目标路径和权限，
  传输后确认目标仍被 `.gitignore` 覆盖，不把敏感内容放进命令输出。
- 数据库变更必须有迁移文件；对外接口使用 `/api/v1`，不兼容变更需要新版本。
- 文件名、变量名和接口字段使用清楚、常见的英文；用户可见文案默认使用简体中文。
- 只在逻辑本身不容易看懂时写注释，不写重复代码含义的注释。

## 完成标准

提交前至少在 `ustc` 服务器运行受影响工作区的类型检查、单元测试和构建。涉及页面时还要在服务器用 Playwright 检查桌面与手机
视口，确认无文字溢出、控件重叠、空白编辑器或不可操作状态。涉及权限时必须覆盖以下反例：

- 作者猜测其他题目编号读取题面、附件或导出包；
- 被禁止登录或明确拒绝某权限的用户仍从角色获得允许；
- 机器人调用删除、权限管理或系统管理接口；
- 导出任务创建后权限被撤销，旧下载地址仍可使用；
- 插件试图跳过核心权限检查或修改被冻结字段。
